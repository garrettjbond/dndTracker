// import SectionTitle from "../../components/SectionTitle";

const Header = () => {
  return (

    <div className="h-screen bg-gradient-to-b from-primary to-white flex flex-col justify-center items-center">
      <h1 className="font-['UnifrakturCook'] text-[12rem] text-black">D&D</h1>
      <h2 className="font-['Splash'] text-[4rem] text-white -mt-24 drop-shadow-lg">Initiative Tracker</h2>
    </div>
  );
};

export default Header;
